const x = 50;
const y = 100;
const obj = { x: x, y: y };
const name = "karim";
const area = "dhaka";

// shortcut
const obj1 = { x, y };

const nobab = {
  name: name,
  area: area,
};

const nobab2 = { name, area };
